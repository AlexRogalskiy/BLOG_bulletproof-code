const TwoColumnEBook = ({ eBookDetails }) => {
  const { title, image, ctaCopty, wordCount, mainFeatures, optInLink } =
    eBookDetails;
  return <div>Enter</div>;
};

export default TwoColumnEBook;
